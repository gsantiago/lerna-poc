import React from 'react';
import ReactDOM from 'react-dom';
import FooBar from '@root/common/components/FooBar';

ReactDOM.render(
  <React.StrictMode>
    <FooBar title="Aplicação Portabilidade" />
  </React.StrictMode>,
  document.getElementById('root')
);
